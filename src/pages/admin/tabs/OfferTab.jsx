import styles from '../Admin.module.css'

/** Placeholder until Task 26 delivers the versioned offer builder. */
export default function OfferTab() {
  return (
    <div className={styles.section}>
      <h2 className={styles.sectionTitle}>Offer</h2>
      <p className={styles.muted}>
        The structured offer builder and version history are being prepared.
      </p>
    </div>
  )
}
